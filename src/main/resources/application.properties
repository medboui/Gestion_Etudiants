spring.datasource.url = jdbc:mysql://localhost:3306/EtudiantDB?createDatabaseIfNotExist=true
server.port:8089
# Username and password
spring.datasource.username = root
spring.datasource.password = 

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
#spring.datasource.validationQuery = SELECT 1
spring.jpa.show-sql = true

spring.jpa.hibernate.ddl-auto = create

# Naming strategy
#spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
#spring.thymeleaf.cache=false
multipart.maxFileSize: 10000KB
multipart.maxRequestSize: 10000KB 
#on a precise la limite du taille du fichier image et la taille max du request par ce que on peut bien envoyer un ensemble de fichier
#et il faut que cet ensemble de fichier sera limite à 10000
#dir.images=E:/JeeProjects/MyNewWorspace/scolarity/
dir.images=External-resources/etPhotos/
dir.files=External-resources/etFiles/